package com.collegeadmissionapp.service;


import org.springframework.stereotype.Service;



@Service
public class ApplicationService {
	
//	public void dashboard() {
//		@Autowired
//		JdbcTemplate jdbcTemplate;
//		 
//		String query="SELECT admissionapp_user.first_name,admissionapp_user.last_name,admissionapp_user.ROLE,admissionapp_user.contact_number,admissionapp_user.user_email,admissionapp_user.user_password,admission.dob,admission.nationality,admission.gender,admission.department,admission.appliaction_status,admission.address,admission.district,admission.state FROM admissionapp_user INNER JOIN admission ON admissionapp_user.id=admission.user_id";
//		return jdbcTemplate.execute(query);
//	}
	
	
}
